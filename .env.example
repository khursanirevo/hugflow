# =============================================================================
# HUGFLOW ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and uncomment the settings you need.
# Only HF_TOKEN is required - everything else has sensible defaults!

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Hugging Face API token (REQUIRED - can't download without it)
# Get from: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_huggingface_token_here


# =============================================================================
# OPTIONAL SETTINGS - Uncomment if needed
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub Authentication (Choose ONE method)
# -----------------------------------------------------------------------------

# Method 1: GitHub Personal Access Token
# Use this for CI/CD or if you want to test PR operations locally
# Get from: https://github.com/settings/tokens (needs 'repo' scope)
# GITHUB_TOKEN=ghp_your_github_token_here

# Method 2: GitHub CLI (RECOMMENDED for local development)
# No need to set anything here! Just run: gh auth login
# Hugflow will auto-detect and use gh CLI if available.

# GitHub repository (auto-detected in CI, only needed for local testing with GitHub features)
# GITHUB_REPO=your-org/hugflow


# -----------------------------------------------------------------------------
# Slack Notifications (OPTIONAL)
# -----------------------------------------------------------------------------

# Enable Slack progress updates (leave commented to disable)
# Get from: Slack App settings → Incoming Webhooks
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T00/B00/XXXXXXXXXXXX
# SLACK_CHANNEL=#ml-datasets


# -----------------------------------------------------------------------------
# Custom Storage Paths (Only if you want to change defaults)
# -----------------------------------------------------------------------------

# Where to store downloaded datasets (default: ./datasets)
# STORAGE_ROOT=./datasets

# Where to store state/manifests (default: .hugflow-state)
# STATE_DIR=.hugflow-state


# -----------------------------------------------------------------------------
# Download Behavior (Only if you want to change defaults)
# -----------------------------------------------------------------------------

# How often to post progress updates (default: every 10,000 files)
# PROGRESS_INTERVAL=10000
# PR_COMMENT_INTERVAL=10000
# SLACK_NOTIFICATION_INTERVAL=10000

# Concurrent download limit (default: 5)
# MAX_CONCURRENT_DOWNLOADS=5

# Network timeouts (defaults: 30s connection, 300s read)
# CONNECTION_TIMEOUT=30
# READ_TIMEOUT=300

# Retry settings (default: 3 retries with 2x backoff)
# MAX_RETRIES=3
# RETRY_BACKOFF=2


# -----------------------------------------------------------------------------
# Validation Settings (Only if you want to change defaults)
# -----------------------------------------------------------------------------

# Check for duplicate datasets (default: true)
# CHECK_DUPLICATES=true

# Storage quota limit (default: 0 = unlimited)
# STORAGE_QUOTA_GB=0

# Validate HF ID exists before downloading (default: true)
# VALIDATE_HF_ID=true


# -----------------------------------------------------------------------------
# Logging (Only if you want to change defaults)
# -----------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
# LOG_LEVEL=INFO

# Log file path (default: empty = stdout only)
# LOG_FILE=

# Log format: json or text (default: json)
# LOG_FORMAT=json


# -----------------------------------------------------------------------------
# Behavior Settings (Only if you want to change defaults)
# -----------------------------------------------------------------------------

# Auto-merge successful PRs (default: false - requires manual merge)
# Uncomment to enable automatic merging on successful download
# AUTO_MERGE=true

# Auto-cleanup YAML files after successful download (default: true)
# AUTO_CLEANUP_REQUESTS=true

# Delete progress files after success (default: true)
# CLEANUP_PROGRESS_ON_SUCCESS=true


# =============================================================================
# SUMMARY
# =============================================================================
# Quick start - just set your HF token:
#
#   HF_TOKEN=hf_your_actual_token_here
#
# That's it! Everything else uses sensible defaults.
# Uncomment optional settings above only if you want to customize behavior.
